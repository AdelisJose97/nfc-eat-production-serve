/* import React, { createContext, useReducer, useEffect } from 'react'

const initialValues = {
  sectionsPosition : []
}

export const Context = createContext(initialValues)
Pendiente por completar funcionalidad

 */